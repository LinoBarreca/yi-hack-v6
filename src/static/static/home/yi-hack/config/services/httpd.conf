# Web interface.
# PORT          - TCP port (default 80).
# USER/PASSWORD - login for the web interface (leave empty for no login).
#                 Independent from the RTSP/ONVIF credentials.
ENABLED=yes
PORT=80
USER=
PASSWORD=
