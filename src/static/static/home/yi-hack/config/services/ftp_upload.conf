# Upload recorded videos to an FTP server.
# HOST                     - remote FTP server address.
# DIR                      - remote directory (created if missing; empty = root).
# DIR_TREE                 - recreate the camera's folder tree on the server (yes/no).
# USERNAME/PASSWORD        - login for the remote FTP server.
# FILE_DELETE_AFTER_UPLOAD - delete the local copy after a successful upload (yes/no).
ENABLED=no
HOST=
DIR=
DIR_TREE=no
USERNAME=
PASSWORD=
FILE_DELETE_AFTER_UPLOAD=no
