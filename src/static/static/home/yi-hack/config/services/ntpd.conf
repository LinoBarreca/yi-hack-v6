# Network time synchronization.
# Enable only if you disabled the Yi cloud apps: otherwise the time comes from
# the Yi servers, and a local NTP daemon would fight with it.
# SERVER - NTP server to use.
ENABLED=no
SERVER=pool.ntp.org
