# WiFi credentials (class 1, overridable from SD/CIFS via apply_config).
# Empty SSID -> wifi_up.sh falls back to the credentials in mtdblock2 (stock QR pairing).
# A non-empty SSID here overrides mtdblock2 (written offset-precise at boot by wifi_up.sh).
SSID=
PSK=
