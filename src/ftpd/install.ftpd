#!/bin/bash

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd $SCRIPT_DIR

# Compress the file
#upx ./_install/sbin/pure-ftpd

mkdir -p ../../build/extra/bin/

cp -a ./_install/sbin/pure-ftpd ../../build/extra/bin/

