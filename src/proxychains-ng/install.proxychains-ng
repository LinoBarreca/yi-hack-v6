#!/bin/bash

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd $SCRIPT_DIR

# Compress the file
#upx ./_install/bin/*
#upx ./_install/lib/*

mkdir -p ../../build/extra/bin/
mkdir -p ../../build/extra/lib/
mkdir -p ../../build/extra/etc/

cp -a ./_install/tmp/sd/yi-hack/bin/* ../../build/extra/bin/
cp -a ./_install/tmp/sd/yi-hack/lib/* ../../build/extra/lib/
cp -a ./_install/tmp/sd/yi-hack/etc/* ../../build/extra/etc/
